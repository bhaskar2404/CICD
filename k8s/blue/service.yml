apiVersion: v1
kind: Service

metadata:
  name: springboot-blue-service

spec:

  type: NodePort

  selector:
    app: springboot-app
    color: blue

  ports:

    - port: 8080
      targetPort: 8080
      nodePort: 30080