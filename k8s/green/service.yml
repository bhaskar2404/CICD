=apiVersion: v1
kind: Service

metadata:
  name: springboot-green-service

spec:
  type: ClusterIP

  selector:
    app: springboot-app
    color: green

  ports:
    - port: 8080
      targetPort: 8080